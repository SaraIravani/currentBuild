pipeline{
    agent any
    stages{
        stage{
            steps{
                sh 'echo Hello world'
                echo "Build number is ${currentBuild.number}"
            }
        }
    }
}